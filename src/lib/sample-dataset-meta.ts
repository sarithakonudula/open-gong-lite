/** Counts for the dummy dataset — safe to import from client components. */
export const SAMPLE_DATASET = {
  companies: 24,
  calls: 42,
  sales: 17,
  customerSuccess: 14,
  support: 11,
} as const;
