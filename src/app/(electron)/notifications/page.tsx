import { NotificationsClient } from "@/components/electron/NotificationsClient";

export const metadata = { title: "notifications — electron" };

export default function Page() {
  return <NotificationsClient />;
}
